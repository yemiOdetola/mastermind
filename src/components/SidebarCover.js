import { React } from "module";

const Peg = (props) => {
    console.log(props);
    return (
        <div className="emptyPeg" pegId={this.props.pegId} pegBgColor={this.props.pegBgColor}></div>
    )
}

export default Peg;